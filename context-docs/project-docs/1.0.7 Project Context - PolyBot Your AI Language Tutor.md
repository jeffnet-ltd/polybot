**Version:** 1.0.7 (GPU Accelerated) **Status:** Development - Implementing Curriculum Data **Last Updated:** 2025-11-27 **Dev Environment:** HP EliteBook 820 G4 / Windows 11 WSL2 (NVIDIA RTX 2080 Ti)

## Executive Summary

Polybot is an **AI-powered multilingual language learning platform** that runs entirely locally. It provides structured conversational practice based on the **CEFR** curriculum.

**Key Differentiator:** True bilingual learning with zero API costs and complete privacy.

**Current Status:** The system is fully operational and **GPU Accelerated**. The backend successfully loads the **BLOOM-3B (6GB)** model into the NVIDIA RTX 2080 Ti VRAM, providing near-instant inference speeds.

## Technical Architecture

### Stack Overview

```
Frontend: React 18.2.0 + Tailwind CSS + Lucide React
Backend: FastAPI (Python 3.11) + BLOOM AI (GPU Accelerated)
Database: MongoDB 7.0
Infrastructure: Docker + Docker Compose (V2)
AI Model: BLOOM 3b (Loaded on NVIDIA CUDA)
```

### System Architecture

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│   React     │────▶│   FastAPI   │────▶│   MongoDB   │
│  Frontend   │      │   Backend   │      │   Database  │
│  Port 3000  │      │  Port 8000  │      │ Port 27017  │
└─────────────┘      └─────────────┘      └─────────────┘
                            │
                            ▼
                     ┌─────────────┐
                     │  BLOOM 3B   │
                     │ (CUDA GPU)  │
                     └─────────────┘
```

## Update Log

**Session: GPU Acceleration**

- **Upgrade:** Switched model from `bloom-560m` (CPU) to `bloom-3b` (GPU).
    
- **Fix:** Migrated to Docker Desktop to resolve WSL2 `libnvidia-ml.so` driver conflicts.
    
- **Fix:** Configured `docker-compose.yml` with `deploy.resources.reservations` for NVIDIA driver.
    
- **Result:** Backend logs confirm `✅ Successfully loaded model 'bigscience/bloom-3b' onto cuda.`
    

**Session: CEFR & Persistence**

- **Pivot:** Shifted to CEFR Curriculum.
    
- **Feature:** Implemented User Registration/Login with MongoDB persistence.
    
- **Fix:** Implemented Background Model Loader to prevent health-check timeouts.
    

## Current Critical Issues

- **Empty Curriculum:** The database has user profiles, but the **Lessons** collection is empty. The frontend displays hardcoded placeholder lessons.
    
- **Generic AI:** The AI prompt is still generic. It needs to be dynamic based on the specific lesson the user selects.
    

## Next Steps (Immediate Focus)

1. **Seed Curriculum Data:** Update `server.py` to define the `Lesson` schema and auto-populate MongoDB with the A1 curriculum (Greetings, To Be, etc.) on startup.
    
2. **Serve Lessons:** Create `GET /lessons` endpoint.
    
3. **Frontend Integration:** Update `App.jsx` to fetch lessons from the API and pass the `lesson_context` to the chat.